/* eslint-disable import/prefer-default-export */
type IRole = {
  status: string;
  id: string;
  createdAt: string;
  name: string;
  description: string;
  code: string;
};

export type { IRole };
